import { DateOnlyDirective } from './date-only.directive';

describe('DateOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new DateOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
